name: Update Recent Blog Posts

on:
  push:
    branches: [master]
  schedule:
    - cron:  40 * * * *

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: JasonEtco/rss-to-readme@v1
        with:
          feed-url: http://caibaojian.com.cn/feed?d
          readme-section: feed
